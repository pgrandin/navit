machine:
  pre:
    - mkdir ~/assets/
    - curl -k -L -o ~/assets/planet-150202.osm.bz2 ftp://ftp.spline.de/pub/openstreetmap/planet/2015/planet-150202.osm.bz2 -O /home/ubuntu/.m2/planet-150202.osm.bzz

dependencies:
  cache_directories:
    - "~/assets/"
