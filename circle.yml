machine:
  pre:
    - mkdir ~/assets/
    - curl -k -L -o ~/assets/planet-150202.osm.bz2 ftp://ftp.spline.de/pub/openstreetmap/planet/2015/planet-150202.osm.bz2
    - cd ~/assets/ && bunzip2 -vv -k planet-150202.osm.bz2
dependencies:
  cache_directories:
    - "~/assets/"
