dependencies:
  pre:
    - "[ -d ~/assets ] || mkdir ~/assets"
    - wget -c -O ~/assets/cov-analysis-linux64-7.6.0.tar.gz http://sd-55475.dedibox.fr/cov-analysis-linux64-7.6.0.tar.gz
    - wget -c -O ~/assets/libspotify-12.1.51-Linux-x86_64-release.tar.gz https://developer.spotify.com/download/libspotify/libspotify-12.1.51-Linux-x86_64-release.tar.gz
    - wget -c -O ~/assets/california-latest.bin https://circle-artifacts.com/gh/navit-gps/navit/43/artifacts/0/home/ubuntu/assets/binmaps/california-latest.bin
    - tar xvfz ~/assets/libspotify-12.1.51-Linux-x86_64-release.tar.gz
    - cd libspotify-12.1.51-Linux-x86_64-release && sudo make prefix=/usr/local install
    - sudo apt-get install cmake
    - sudo apt-get install libpng12-dev 
    - sudo apt-get install libgtk2.0-dev 
    - sudo apt-get install librsvg2-bin 
    - sudo apt-get install libfreetype6-dev
    - sudo apt-get install libdbus-glib-1-dev
    - sudo apt-get install g++
    - sudo apt-get install libjansson-dev
    - ls 
    - ls navit
    - bash ~/navit/build.sh
  cache_directories:
    - "~/assets/"
test:
  post:
    - sed -i -e 's@name="Local GPS" profilename="car" enabled="yes" active="1"@name="Local GPS" profilename="car" enabled="no" active="0"@' navit.xml:
        pwd: navit/bin/navit/
    - sed -i -e 's@name="Demo" profilename="car" enabled="no" active="yes"@name="Demo" profilename="car" enabled="yes" active="yes" follow="1" refresh="1"@' navit.xml:
        pwd: navit/bin/navit/
    - sed -i -e 's@type="internal" enabled@type="internal" fullscreen="1" font_size="350" enabled@' navit.xml:
        pwd: navit/bin/navit/
    - sed -i -e 's@libbinding_dbus.so" active="no"@libbinding_dbus.so" active="yes"@' navit.xml:
        pwd: navit/bin/navit/
    - echo '<map type="binfile" data="~/assets/california-latest.bin" />' > /home/ubuntu/navit/navit/bin/navit/maps/planet.xml
    - ./navit:
        pwd: navit/bin/navit/
        background: true
    - sleep 5
    - import -window root $CIRCLE_ARTIFACTS/default.png
    - bash test_route.sh $CIRCLE_ARTIFACTS
    - dbus-send  --print-reply --session --dest=org.navit_project.navit /org/navit_project/navit/default_navit org.navit_project.navit.navit.quit
deployment:
  deploy_staging:
    branch: staging
    commands:
      - git push origin $CIRCLE_SHA1:refs/heads/master
