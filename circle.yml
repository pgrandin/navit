#machine:
#  services:
#    - docker
dependencies:
  pre:
    - wget -c -O ~/assets/cov-analysis-linux64-7.6.0.tar.gz http://sd-55475.dedibox.fr/cov-analysis-linux64-7.6.0.tar.gz
    - wget -c -O ~/assets/libspotify-12.1.51-Linux-x86_64-release.tar.gz https://developer.spotify.com/download/libspotify/libspotify-12.1.51-Linux-x86_64-release.tar.gz
    - tar xvfz ~/assets/libspotify-12.1.51-Linux-x86_64-release.tar.gz
    - cd libspotify-12.1.51-Linux-x86_64-release && sudo make prefix=/usr/local install
#    - sudo apt-get install cmake zlib1g-dev libpng12-dev libgtk2.0-dev librsvg2-bin g++
#    - sudo apt-get upgrade
    - sudo apt-get install cmake 
#    - sudo apt-get install zlib1g-dev
    - sudo apt-get install libpng12-dev 
    - sudo apt-get install libgtk2.0-dev 
    - sudo apt-get install librsvg2-bin 
    - sudo apt-get install libfreetype6-dev
    - sudo apt-get install libdbus-glib-1-dev
    - sudo apt-get install g++
    - sudo apt-get install libjansson-dev
    - ls 
    - ls navit
    - bash ~/navit/build.sh
  cache_directories:
    - "~/assets/"
#    - "~/docker"
#  override:
#    - if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi
#    - docker build -t navit/circleci .
#    - mkdir -p ~/docker; docker save navit/circleci > ~/docker/image.tar
test:
  post:
    - cd ~/navit/navit/bin/navit/ && sed -i -e 's@libbinding_dbus.so" active="no"@libbinding_dbus.so" active="yes"@' navit.xml
    - cd ~/navit/navit/bin/navit/ && DISPLAY=:99 ./navit &
    - sleep 5 && DISPLAY=:99 import -window root ~/assets/default.png
    - pkill navit
#    - DISPLAY=:99 dbus-send  --print-reply --session --dest=org.navit_project.navit /org/navit_project/navit/default_navit org.navit_project.navit.gui.command string:"quit()"
